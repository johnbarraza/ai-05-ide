# Handwritten derivation

Add **one real photo of your own handwritten derivation** as `derivation.jpg` (or `derivation.png`). A generated image, SVG, screenshot of LaTeX, or typed page does not satisfy the requirement.

Write the following discrete three-type argument on one sheet.

1. State (L<M<H), (a=z_{AI}), (0<h<1), and
   \[
   h n_L(1-z_L)=1
   \quad\Longrightarrow\quad
   n_L=\frac{1}{h(1-z_L)}.
   \]
2. Autonomous AI has (r^*=a) under abundant compute. From zero profit,
   \[
   n_L(a-w_L^A)-a=0
   \quad\Longrightarrow\quad
   w_L^A=a[1-h(1-z_L)].
   \]
3. Compare with the pre-AI wage:
   \[
   w_L^A>w_L^0
   \iff
   a>\bar a_L\equiv\frac{w_L^0}{1-h(1-z_L)}.
   \]
4. If non-autonomous AI is adopted, (r^\star=0), so
   \[
   n_L(a-w_L^N)=0
   \Longrightarrow w_L^N=a,
   \qquad
   w_L^N-w_L^A=ah(1-z_L)>0.
   \]
5. End with this verdict in your own handwriting: capability determines whether the bottom-winner/adoption thresholds are crossed; autonomy determines the roles available to AI and the rent wedge.

The full Spanish checklist is at `../QUE_HACER_A_MANO.md` in the private `.me/semana_5/` workspace. After adding the real photo, recompile the deck and confirm that it appears on the “Where I did not believe the AI” slide.
